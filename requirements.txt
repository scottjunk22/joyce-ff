# Phase 1 (scoring engine + tests) needs only pytest — the engine itself has
# zero runtime dependencies on purpose.
pytest>=8.0

# Data layer (Phase 1 validation + Phase 2 sync).
# NOTE: we deliberately do NOT use nfl_data_py — it pins an old pandas with no
# Python 3.12 wheel and fails to build. Instead we read nflverse's published
# release files (play-by-play parquet + schedules csv) directly. Same data.
pandas>=2.2            # dataframe handling for nflverse pulls
pyarrow>=15            # read nflverse parquet releases
requests>=2.31         # (available for future polite fetching)
beautifulsoup4>=4.12   # defensive HTML parsing of the hand-maintained site
flask>=3.1             # league web app (stevejoyceff.com)
